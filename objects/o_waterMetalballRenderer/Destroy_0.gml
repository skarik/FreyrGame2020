/// @description Free all the buffers

surface_free(m_surface);
surface_free(m_surfacePeturb);

surface_free(m_surfaceReflect);
surface_free(m_surfaceReflect_Temp);
surface_free(m_surfaceDepthHack);