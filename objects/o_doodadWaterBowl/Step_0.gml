/// @description Update motion

event_user(0);

/*if (visible)
{
	// Do motion
	x += xspeed * Time.deltaTime;
	y += yspeed * Time.deltaTime;
	
	// Do collision with other bowls
	var bowl_check = id;
	with (o_doodadWaterBowl)
	{
		var dist_sqr = sqr(bowl_check.x - x) + sqr(bowl_check.y - y);
	}
}*/

depthUpdate();