var buffer = argument0;

buffer_write(buffer, buffer_f32, x);
buffer_write(buffer, buffer_f32, y);
buffer_write(buffer, buffer_f64, m_wateredTime);
buffer_write(buffer, buffer_bool, tilled);
buffer_write(buffer, buffer_bool, watered);